PL.poland:

  schedule:
    crontab: '0 * * * *'

  pipeline:
    - run: simple_remote_source
    - run: validate_tabular_remote_source
    - run: dump
      parameters:
          out-file: extracted.data